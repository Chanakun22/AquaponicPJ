# Smart Aquaponics Test Guide

คู่มือนี้เขียนสำหรับผู้ทดสอบที่ไม่ได้รู้โครงสร้างระบบภายในมาก่อน เป้าหมายคือให้เปิดระบบแล้วทดสอบได้ทีละขั้นโดยไม่ต้องเดา

## 1. เป้าหมายของการทดสอบ

ผู้ทดสอบต้องตอบให้ได้ 3 ข้อ

- ระบบเปิดติดและสื่อสารกับหน้าเว็บได้หรือไม่
- เซ็นเซอร์และอุปกรณ์สั่งงานได้จริงหรือไม่
- เมื่อสั่งหยุดฉุกเฉิน ระบบกลับสู่สถานะปลอดภัยหรือไม่

ถ้าตอบครบ 3 ข้อนี้ได้ถือว่าทดสอบระบบพื้นฐานผ่าน

## 2. สิ่งที่ต้องมีก่อนเริ่ม

- ESP32 ถูกแฟลชเฟิร์มแวร์ล่าสุดแล้ว
- Raspberry Pi เปิด dashboard และ MQTT broker อยู่แล้ว
- ผู้ทดสอบเชื่อมต่อเข้าเครือข่ายของระบบได้
- มีสิทธิ์เข้าใช้งานหน้าเว็บแบบ admin
- รู้ตำแหน่งอุปกรณ์จริง: ไฟ, ปั๊ม A, ปั๊ม B, พัดลม, ระบบน้ำ

## 3. หน้าที่ของแต่ละหน้า

- Dashboard `/` ใช้ดูภาพรวมว่าระบบ online และค่าหลักอัปเดตอยู่
- Settings `/settings` ใช้ดูและปรับค่าควบคุม
- Hardware Test `/hwtest` ใช้ทดสอบอุปกรณ์ทีละตัว
- Full Logs `/full_logs` ใช้ดูข้อความผิดพลาดหรือผลการทำงานย้อนหลัง

ถ้าเป็นผู้ทดสอบใหม่ ให้เริ่มที่ Dashboard แล้วไป Hardware Test ก่อนทุกครั้ง

## 4. กฎความปลอดภัยก่อนทดสอบ

- ทดสอบปั๊มทีละตัว ห้ามสั่งพร้อมกัน
- ขณะทดสอบพัดลมหรือปั๊ม ต้องมีคนมองอุปกรณ์จริงอยู่หน้างาน
- ถ้าอุปกรณ์ใดทำงานไม่หยุด ให้กด Safe Stop ทันทีจากหน้า Hardware Test
- ถ้าระบบขึ้น alarm หรือรีบูตเอง ให้หยุดทดสอบและบันทึกเวลา/อาการ

## 5. ลำดับการทดสอบที่แนะนำ

### Step 1: ตรวจว่า ESP ออนไลน์

หน้า: Dashboard `/`

ให้ตรวจ 3 จุด

- `ESP32 STATUS` ต้องเป็น `ONLINE`
- `MQTT` ต้องไม่ขึ้น `Offline` ถ้าระบบ Pi ทำงานปกติ
- ค่าหลักเช่นอุณหภูมิ น้ำ, อากาศ, ความชื้น ต้องไม่ค้างทั้งหมด

ผ่านเมื่อ

- หน้า Dashboard อัปเดตข้อมูลได้ต่อเนื่องภายในไม่กี่วินาที

ไม่ผ่านเมื่อ

- ESP ขึ้น `OFFLINE`
- ค่าทุกตัวเป็นศูนย์หรือไม่เปลี่ยนเลย

### Step 2: อ่านค่าเซ็นเซอร์แบบ snapshot

หน้า: Hardware Test `/hwtest`

กดปุ่มอ่านค่าเซ็นเซอร์หนึ่งครั้ง แล้วดูค่าเหล่านี้

- Water Temperature
- Air Temperature
- Humidity
- TDS
- pH
- Light

ผ่านเมื่อ

- มีค่าแสดงครบและดูสมเหตุผลกับสภาพแวดล้อมจริง

ไม่ผ่านเมื่อ

- ค่าเป็น `N/A`, ศูนย์ทุกตัว, หรือค้างเหมือนเดิมแม้เปลี่ยนสภาพแวดล้อม

### Step 3: ทดสอบไฟ

หน้า: Hardware Test `/hwtest`

กดเปิดไฟ แล้วดูว่า relay/ไฟจริงติด
กดปิดไฟ แล้วดูว่าดับจริง

ผ่านเมื่อ

- สถานะบนหน้าเว็บตรงกับอุปกรณ์จริง

### Step 4: ทดสอบปั๊มโดส A

หน้า: Hardware Test `/hwtest`

กด `Run Pump A`
รอให้ระบบรายงานว่าจบ

ผ่านเมื่อ

- ปั๊ม A ทำงานจริง
- หน้าเว็บขึ้นสถานะ running แล้ว completed
- ปั๊มหยุดเองตามเวลา test

ไม่ผ่านเมื่อ

- ปั๊มไม่ติด
- ติดแล้วไม่ยอมหยุด
- สถานะบนหน้าเว็บไม่ตรงกับของจริง

### Step 5: ทดสอบปั๊มโดส B

ทำเหมือน Step 4 แต่ใช้ `Run Pump B`

ผ่านเมื่อ

- ปั๊ม B ทำงานและหยุดปกติ

### Step 6: ทดสอบพัดลม

หน้า: Hardware Test `/hwtest`

ทดสอบตามลำดับนี้

- `Fan ON`
- `Fan OFF`
- `Fan AUTO`

ผ่านเมื่อ

- ON แล้วพัดลมหรือ relay พัดลมติดจริง
- OFF แล้วดับจริง
- AUTO แล้วสถานะกลับเป็น auto mode

### Step 7: ทดสอบระบบน้ำ

หน้า: Hardware Test `/hwtest`

ทดสอบเฉพาะ output ที่ต่อจริงในระบบ แล้วดูสถานะด้านขวาประกอบเสมอ

รายการที่ควรเช็ก

- circulation on/off
- refill on/off
- clear alarm
- route fish / route sump ถ้าระบบมี route valve

ผ่านเมื่อ

- output จริงทำงานตรงกับคำสั่ง
- state และ reason บนหน้าเว็บอธิบายสถานะได้ถูก

### Step 8: ทดสอบ Safe Stop

หน้า: Hardware Test `/hwtest`

กด `Safe Stop` หนึ่งครั้ง

ผลที่คาดหวัง

- ปั๊มโดสหยุด
- ไฟดับ
- พัดลมหยุด
- manual refill ปิด
- auto refill ถูกปิดไว้เพื่อความปลอดภัย

ผ่านเมื่อ

- ทุก output กลับสู่สถานะปลอดภัย
- มีข้อความเตือนหรือสถานะบอกว่าระบบถูกหยุดแล้ว

### Step 9: ทดสอบการตั้งค่าจากหน้า Settings

หน้า: Settings `/settings`

ให้ลองเปลี่ยนเฉพาะค่าที่เข้าใจง่ายก่อน

- fan enable/disable
- fan auto/manual
- water options ที่ใช้งานจริง
- automation targets ถ้ามีผู้ดูแลระบบกำหนดช่วงไว้แล้ว

ทุกครั้งที่กด apply ให้เช็ก 3 อย่าง

- หน้าเว็บขึ้น success
- ESP สะท้อนสถานะใหม่กลับมา
- ค่าไม่เด้งกลับเป็นค่าเก่าเอง

### Step 10: กลับไปดู Dashboard อีกรอบ

หน้า: Dashboard `/`

ตรวจว่า

- ระบบยัง online
- ค่า sensor ยังอัปเดต
- สถานะ automation, water, fan ตรงกับสิ่งที่เพิ่งทดสอบ

## 6. ตารางบันทึกผลแบบง่าย

| รายการ | Pass/Fail | หมายเหตุ |
| --- | --- | --- |
| ESP Online |  |  |
| Sensor Snapshot |  |  |
| Light ON/OFF |  |  |
| Pump A |  |  |
| Pump B |  |  |
| Fan ON/OFF/AUTO |  |  |
| Water Control |  |  |
| Safe Stop |  |  |
| Settings Apply |  |  |
| Dashboard Realtime |  |  |

## 7. ถ้าเจอปัญหาให้ทำอย่างไร

ถ้าเจออาการต่อไปนี้ ให้หยุดการทดสอบแล้วแจ้งผู้ดูแลระบบ

- หน้าเว็บขึ้น offline ตลอด
- ปั๊มหรือพัดลมไม่หยุดหลังสั่ง stop
- Dashboard ไม่อัปเดตแต่หน้าอื่นยังตอบ
- ค่า sensor กระโดดผิดธรรมชาติหรือหายทั้งชุด
- ระบบรีบูตเอง

ข้อมูลที่ควรส่งให้ผู้ดูแลระบบ

- เวลาที่เกิดปัญหา
- หน้าที่กำลังใช้งาน
- ปุ่มหรือ action ที่กดก่อนเกิดปัญหา
- สิ่งที่คาดหวัง
- สิ่งที่เกิดขึ้นจริง

## 8. สรุปสำหรับผู้ทดสอบใหม่

ถ้าจำได้แค่ลำดับเดียว ให้ใช้ลำดับนี้

1. เปิด Dashboard ดูว่า online
2. เข้า Hardware Test อ่าน sensor
3. ทดสอบไฟ
4. ทดสอบ Pump A แล้ว Pump B
5. ทดสอบ Fan ON/OFF/AUTO
6. ทดสอบ water controls ที่มีจริง
7. กด Safe Stop
8. ไปดู Settings และ Dashboard ยืนยันผลอีกครั้ง

ถ้าระบบผ่านครบตามนี้ ถือว่าพร้อมใช้งานระดับพื้นฐาน